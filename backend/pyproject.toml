[tool.poetry]
name = "outfiq-backend"
version = "0.1.0"
description = "AI Fashion Studio Backend"
authors = ["AI <ai@example.com>"]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
